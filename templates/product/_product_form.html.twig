{{ form_start(form, {'attr': {'class': 'row', 'id': 'product'}}) }}

<div class="col-sm-6 col-md-6">
    {{ form_row(form.name) }}
</div>
<div class="col-sm-6 col-md-3">
    {{ form_row(form.description) }}
</div>
<div class="col-sm-6 col-md-3">
    {{ form_row(form.price) }}
</div>
<div class="col-sm-6 col-md-3">
    {{ form_row(form.image) }}
</div>

{{ form_end(form) }}

<button form="product" name="save-button" class="btn btn-sm btn-primary"><i class="fas fa-save"></i> Сохранить</button>